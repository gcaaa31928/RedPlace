<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorButtonNormal">@color/colorPrimary</item>
        <item name="android:colorBackground">@color/background_material_light</item>
        <item name="android:textColorPrimary">@color/cast_expanded_controller_progress_text_color</item>
        <item name="android:textColorPrimaryInverse">
            @color/cast_intro_overlay_button_background_color
        </item>
        <item name="android:textColorSecondaryInverse">@color/colorPrimary</item>
    </style>

</resources>
